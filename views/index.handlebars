<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="input">
                <h2 class="heading" id="add">Lets Make A New One</h2>
                <div class="form-group">
                    <form method="POST" action="api/burgers" class="addOne">
                        <textarea id="textArea" type="text" name="burger_name" rows="2" cols="40"></textarea>
                        <button id="btnCreate" type="submit">Ding Burger Done!</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <div class="main row">
        <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3">
            <div class="leftBurgers">
                <h2 class="heading">Hot and Ready</h2>
                <ul>
                    {{#each burgers}} {{#unless this.devoured}}
                        <li>
                            <p class="burgerType">{{this.burger_name}}</p>
                            <form action="api/burgers/{{this.id}}?_method=PUT" method="POST" class="dev">
                                <input type="hidden" name="devoured" value="true">
                                <button class="btnDevour" type="submit">Devour it</button>
                            </form>
                        </li>
                    {{/unless}} {{/each}}
                </ul>
            </div>
        </div>
        <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3">
            <div class="rightBurgers">
                <h2 class="heading">All Gone</h2>
                <ul>
                    {{#each burgers}} {{#if this.devoured}}
                    <li>
                        <p class="burgerType">{{this.burger_name}}</p>
                    </li>
                    {{/if}} {{/each}}
                </ul>
            </div>
        </div>
    </div>
</div>